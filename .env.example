# WebMCP.md environment
#
# This is a fully informational site — no payments, no secrets required to run.
#
# Optional: pin the public site URL (used for canonical/OG absolute URLs).
# If unset, the app uses the hard-coded https://www.webmcp.md.
# NEXT_PUBLIC_SITE_URL=https://www.webmcp.md
